{
  "name": "hide-and-seek",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/client.js --bundle --outfile=public/client.js --format=esm --target=es2020",
    "dev": "pnpm build && wrangler dev",
    "deploy": "pnpm build && wrangler deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.33.0",
  "dependencies": {
    "partyserver": "^0.4.1",
    "partysocket": "^1.1.16"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "wrangler": "^4.81.1"
  }
}
